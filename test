# ============================================================
# PREFLIGHT — na Kernel Restart
# ============================================================

import inspect

from azure.ai.documentintelligence.models import DocumentContentFormat

from application.vectordatabase.src.preprocessing.sharepoint_extraction import (
    SharePointDataFetcher,
)

print("✓ Azure Document Intelligence beschikbaar")
print("✓ SharePointDataFetcher beschikbaar")

# Meteen vooruitkijken of onze aannames over de bestaande fetcher kloppen
print("\nConstructor:")
print(inspect.signature(SharePointDataFetcher))

print("\nRelevante bestaande methods:")
print([
    name for name in dir(SharePointDataFetcher)
    if any(word in name.lower() for word in [
        "fetch", "download", "document", "markdown", "load"
    ])
])
