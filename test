import sys
from pathlib import Path

repo_root = Path.cwd().resolve()

if str(repo_root) not in sys.path:
    sys.path.insert(0, str(repo_root))

from application.vectordatabase.src.sharepoint_extraction import (
    SharePointDataFetcher,
)
