import os

from application.models.embedding_model import get_embedding_model
from application.vectordatabase.src.database.vector_database import VectorDatabaseManager
from application.vectordatabase.utils.util_functions import load_config





logger.info("Initializing vectordatabase")

config = load_config(os.environ["CONFIG"])
embedding_model = get_embedding_model()

vd_manager = VectorDatabaseManager(
    db_vendor=config["vectordatabase"]["db_vendor"],
    collection_name=config["vectordatabase"]["collection_name"],
    embedding_function=embedding_model,
)

vector_db = vd_manager.get_vectordatabase()

logger.info("Finished initializing vectordatabase")
