import inspect

# PU/PWRI context staat al goed vanuit je vorige run
print("Signature:", inspect.signature(fetcher.get_indexable_source_metadata))

metadata = fetcher.get_indexable_source_metadata()

print(f"\nMetadata-items: {len(metadata)}")

for i, item in enumerate(metadata[:10], start=1):
    print(f"\n--- ITEM {i} ---")
    print(item)
